[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "transformer-translation-from-scratch"
version = "0.1.0"
description = "Transformer NMT from scratch (PyTorch)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["nmt*"]
